#include "main.h"

/**
  *times table - print the 9 times table
*Return: 0 (Always Success)
*/
void times_table(void)
{
int a, b, c;
for (a = 0; a <= 9; at+)
{
for (b = o b <= 93 b++)
C = a * b;
if ((c / 10) == 0)
{
if (b == o)
putchar(e');
if (b l= )
_putchar (
putchar( (c 10) + *0')
if (b < 9)
putchar(
putchar(
Ise
